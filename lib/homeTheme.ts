// The Home page's single gray tint over the scroll-scrubbed video
// background. Used in exactly one place — the overlay div rendered by
// ScrollFrameBackground — so it can never stack with itself. Every other
// Home-page element (sections, Navbar, Footer) stays fully transparent.
export const HOME_OVERLAY_BG = 'rgba(71, 71, 72, 0.7)';
